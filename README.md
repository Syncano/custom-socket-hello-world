# HelloWorld example of Syncano Custom Socket

This is the very basic example of how Custom Socket works. 
It define one single endpoint in Custom Socket which displays a HelloWorld message - when requested.